[package]
name = "aes-logging"
version.workspace = true
edition.workspace = true

[dependencies]
log = { version = "0.4", features = ["std"] }
uuid = { workspace = true }
chrono = { workspace = true }
